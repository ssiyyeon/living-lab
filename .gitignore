# 원본 민원 데이터 (개인정보 포함 가능) - 로컬에만 보관, 저장소에는 올리지 않음
*.xlsx
*.pdf

# PDF 매뉴얼을 파싱한 원문 (직원 연락처 등 PDF와 동일한 민감정보 포함)
manual/situations.json

# 실제 민원내용 텍스트가 섞인 파생 데이터/로그
analysis/action_categorized.csv
analysis/feedback_log.csv

# 학습된 모델 아티팩트 (model/train_and_save.py로 재생성 가능한 바이너리)
model/artifacts/

# Python
__pycache__/
*.pyc

# OS / 로컬 개인 설정
.DS_Store
.claude/settings.local.json
